describe('Create a new item', () => {
  // beforeEach(() => {
  //   cy.visit('/');
  // });

  // it('should', () => {

  // });
  it('should run the web', () => {
    cy.visit('http://localhost:3000/');
  });
});
